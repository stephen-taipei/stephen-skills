---
description: 掃描專案中適合改用 Rust/WASM 的 CPU 熱點，改造選定項目並以 Web Worker 背景執行，最後產出 PDF 驗收報告
argument-hint: [掃描範圍，例如 src/ 或 apps/mobile；留空則掃整個專案]
---

啟動 **review-rust-wasm** skill，對目前專案執行 Rust/WASM 改造評估與實作流程。

掃描範圍：$ARGUMENTS（未指定時掃描整個專案）

依 skill 定義的六個步驟依序執行，每一步完成後回報再往下走：

1. **掃描候選** —— 產出「建議改造」與「已排除」兩張表，效益須標明實測或估算
2. **用戶選取** —— 以多選方式讓我勾選要動工的項目，不要預設全選
3. **移至 Rust 並編譯 WASM** —— 先備黃金測資與等價性測試，全綠才往下
4. **Web Worker 整合** —— 主執行緒不載入 WASM，並實作 fallback 鏈
5. **Benchmark** —— 量端到端耗時與主執行緒阻塞兩者，不可只報其一
6. **PDF 驗收報告** —— 語言與我提問的語言一致

嚴格遵守 skill 的四條原則：效益必須量測、等價性優先於效能、一定要有 fallback、反指標命中就排除。

若掃描後沒有任何項目值得改造，直接告訴我「這個專案不值得引入 Rust/WASM」並說明原因 —— 不要硬湊項目。
